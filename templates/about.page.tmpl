{{template "base" .}}

{{define "content"}}
    <div class="container">
        <dov class="row">
            <div class="col-12">
                <h1>About</h1>
                <p>This is from the template: {{index .StringMap "test"}}</p>
            </div>
        </dov>
    </div>

{{end}}